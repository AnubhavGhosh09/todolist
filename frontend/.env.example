# Absolute URL of the deployed backend API (used in production builds).
# In development, leave this unset — Vite proxies /api to http://localhost:5000.
# Example: https://your-todo-api.onrender.com/api/v1
VITE_API_URL=
